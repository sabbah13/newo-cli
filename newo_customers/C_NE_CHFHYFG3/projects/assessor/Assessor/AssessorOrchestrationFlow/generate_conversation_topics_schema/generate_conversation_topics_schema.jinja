{{system}}
{
    "type": "object",
    "properties": {
        "intents": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "name": {
                        "type": "string",
                        "description": "Intent title without description"
                    },
                    "auditor_instruction": {
                        "type": "string",
                        "description": "Instructions for the auditor caller what CTA it must achieve, what to tell, what to ask etc, keep it short, but comprehensive."
                    },
                    "checklist": {
                        "type": "array",
                        "items": {
                            "type": "string",
                            "description": "Separate what exactly should happen in the conversation, what steps the `ConvoAgent` should accomplish during executing this intent."
                        },
                        "description": "Enumerate conversational requirements of what exactly ConvoAgent must ask/do within the conversation to reach CTA according to <ConvoAgentInstruction>"
                    },
                    "outcome": {
                        "type": "string",
                        "description": "Describe what has to happen at the end of the conversation, what CTA/end point should be reached according to <ConvoAgentIntentMap>"
                    }
                },
                "required": ["name", "auditor_instruction", "checklist", "outcome"]
            }
        }
    },
    "required": ["intents"]
}
{{end}}
{{Return(val=GetCurrentPrompt())}}
