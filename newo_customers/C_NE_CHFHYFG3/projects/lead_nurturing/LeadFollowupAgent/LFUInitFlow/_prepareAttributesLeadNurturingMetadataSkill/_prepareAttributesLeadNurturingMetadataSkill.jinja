{# Important note: setting Attributes Metadata is a heavy operation so it is performed only one time when it was enabled explicitly #}
{% if GetProjectAttribute(field="project_attributes_setting_init_attributes_metadata_enabled") != "True" %}
    {{Return()}}
{% endif %}


{# BEGIN AUTOGENERATED ATTRIBUTES METADATA #}
{# ### 4.1 lead nurturing Attributes #}

{# #### `project_attributes_setting_lead_nurturing_instructions` #}

{{SetCustomerMetadataAttribute(
    idn="project_attributes_setting_lead_nurturing_instructions",
    field="multiple_fields",
    value={ "title": "Lead Nurturing - Instructions [A]", "description": """Provide a set of actions for lead nurturing after successful CTA. At the end of every session, the
agent will determine if the CTA was achieved and schedule according tasks.

**Example:**

```txt
> Send SMS 4 hours before booking and remind them of appointment
> Call them 1 day after booking and ask their feedback, if they enjoyed everything, have any complaints or suggestions.
```""", "group": "4.1 Lead Nurturing", "value_type": "string", "is_hidden": False }
)}}

{# ### 8. system Attributes #}

{# #### `project_attributes_setting_init_attributes_metadata_enabled` #}

{{SetProjectMetadataAttribute(
    idn="project_attributes_setting_init_attributes_metadata_enabled",
    field="multiple_fields",
    value={ "title": "Project - Init - Setting-Up Attributes Metadata - Enabled [C]", "description": """Metadata for one-time initialization of Attributes Metadata.

This attribute controls the initial setup of attribute metadata. Since this process requires
significant resources, the attribute automatically switches to `False` after the first
initialization.

If you need to update the attribute metadata, you must manually set it to `True` and press the
`Publish` button.""", "group": "8. System", "value_type": "bool", "is_hidden": True }
)}}

{# END AUTOGENERATED ATTRIBUTES METADATA #}
