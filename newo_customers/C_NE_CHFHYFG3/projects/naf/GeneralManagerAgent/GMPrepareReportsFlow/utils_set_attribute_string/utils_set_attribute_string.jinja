{# This skill removes leading and trailing whitespace (including spaces, tabs, and newlines) from a string and set into attribute. #}

{{SetCustomerAttribute(field=attribute_idn, value=value | trim)}}