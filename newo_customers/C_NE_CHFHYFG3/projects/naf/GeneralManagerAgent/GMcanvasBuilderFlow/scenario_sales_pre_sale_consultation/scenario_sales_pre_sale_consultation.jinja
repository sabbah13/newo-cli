{% set title = '## **Scenario 1:** "Pre-Sale Consultation"' %}

{% set body %}
### **Step 1.1:** ConvoAgent needs to consult the user by proactively leading the conversation. In your answer you may only use information available in <BusinessContext> and <AdditionalBusinessContext> sections. If the user’s query is not covered there, say that you cannot answer the question.
[[project_attributes_sales_pre_sale_questions]]
{% endset %}

{% set description %}
I’m sorry, but I can’t complete this request because the <BusinessContext> and <AdditionalBusinessContext> were not provided. Please share those sections (or the relevant pre-sale objectives, audience, and conversation flow) and I’ll craft a concise 1–2 sentence description.
{% endset %}


{% set item = {
    "idn": "scenario_sales_pre_sale_consultation",
    "title": title.strip(),
    "body": body.strip(),
    "mentions": [],
    "description": description.strip(),
    "is_from_library": True,
    "origin_idn": ""
} %}

{{Return(val=json.dumps(item, ensure_ascii=False, indent=2))}}
