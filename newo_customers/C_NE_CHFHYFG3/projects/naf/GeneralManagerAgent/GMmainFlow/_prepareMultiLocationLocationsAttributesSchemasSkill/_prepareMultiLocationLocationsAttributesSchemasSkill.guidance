{{#system~}}
{
  "type": "object",
  "properties": {
    "list": {
      "type": "array",
      "description": "List of indexes for all <Locations>",
      "items": {
        "type": "string",
        "description": "Location index, must be following regex [a-z_]+"
      }
    }
  },
  "required": ["list"]
}
{{~/system}}

{{Return(val=GetCurrentPrompt())}}
