{{#system~}}
{
  "type": "object",
  "properties": {
    "explanation": {
      "type": "string",
      "description": "An explanation of the URL processing result."
    },
    "input_url_is_valid": {
      "type": "boolean",
      "description": "Indicates whether <ProvidedURL> contains a completely valid URL."
    },
    "output_refined_transformed_url": {
      "type": "string",
      "description": "The final refined URL (e.g. `https://example.com/hello_world`)."
    }
  },
  "required": [
    "explanation",
    "input_url_is_valid",
    "output_refined_transformed_url"
  ]
}
{{~/system}}

{{Return(val=GetCurrentPrompt())}}
