{{system}}
You spoke with a customer and {{session_short_description}}.
{{user_behavior_analysis}}.

Write an email in {{GetCustomerAttribute(field="project_business_language_primary")}} to the manager and let them know about this.
Be descriptive and use a natural, human-like tone.

Do NOT include greetings, salutations, or sign-offs in your email. Do not generate subject. Do not include contact information.
{{end}}
{{assistant}}{% set email_body = Gen(temperature=0.2, topP=0.5, maxTokens=4000, skipFilter=True, thinkingBudget=1020) %}{{end}}

{{Return(val=email_body)}}
