{{!-- TODO: pronounce business name using phonetic transcription --}}

{{#system~}}
Your goal is to complete the following objectives:

1. Summarize the current conversation and put the generated summary into `current_session_summary` in English.
2. Define the **main language** from the section <LanguagePreferencesForGreetings> and put it into `preferred_language_for_greetings`.
3. Combine <DisclaimerPhrase> with one of <RecentReturnGreetingPhraseExamples> examples (if there is only one example, use that one) and translate it into the **main language** and put the translation into `recent_return_greeting`.
4. Combine <DisclaimerPhrase> with one of <LaterReturnGreetingPhraseExamples> examples (if there is only one example, use that one) and translate it into the **main language** and put the translation into `later_return_greeting`.

<DisclaimerPhrase>
{{disclaimerPhrase}}
</DisclaimerPhrase>

<LanguagePreferencesForGreetings>
1. First priority - user language: {{userLanguage}}
2. Second priority - business main language: {{GetCustomerAttribute(field="project_business_language_primary")}}
Use the highest priority language available as the **main language** for the greetings.
</LanguagePreferencesForGreetings>

<RecentReturnGreetingPhraseExamples>
{{recent}}
</RecentReturnGreetingPhraseExamples>

<LaterReturnGreetingPhraseExamples>
{{later}}
</LaterReturnGreetingPhraseExamples>

<Conversation>
{{memory}}
</Conversation>
{{~/system}}

{{Return(val=GetCurrentPrompt())}}
